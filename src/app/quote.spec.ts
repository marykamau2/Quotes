import { Quote } from './quote';
import { QuotesComponent } from './quote/quote.component';

describe('Quote', () => {
  it('should create an instance', () => {
    expect(new QuotesComponent()).toBeTruthy();
  });
});
